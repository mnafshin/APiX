name: docs-sync

on:
  push:
    paths:
      - 'docs/**'
      - 'README.md'
  pull_request:
    paths:
      - 'docs/**'
      - 'README.md'

jobs:
  docs-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Go tests
        run: make test
      - name: Run markdown link checker (lychee)
        uses: lycheeverse/lychee-action@v2
        with:
          args: 'docs/**/*.md README.md --root .'
          # pass --root . so lychee can resolve root-relative links like /public/...
      - name: Lint markdown (markdownlint-cli)
        run: |
          # Install markdownlint CLI and run against docs and README
          npm install -g markdownlint-cli@0.33.0
          markdownlint "docs/**/*.md" README.md
